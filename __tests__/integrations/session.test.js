describe('auth', () => {
  it('should return a JWT when its called', () => { //test, KISS

  })
  it('should sum two numbers',()=>{
    const x = 2
    const y = 3
    const sum = x+y
    expect(sum).toBe(5)
  })
})